%div{:class => 'menu'}
  %ul
    %li{:style => 'text-decoration:none'}
      = if current_user.nil?
        = link_to "Login", login_path
      - if signed_in?
        = link_to "Logout" , logout_path

